# RAG 笔记

RAG（Retrieval-Augmented Generation）先检索相关知识，再把检索结果放进提示词，让模型基于证据回答，而不是只靠参数记忆。

典型链路：文档解析 → 切分（chunking）→ 建索引 → 查询检索 →（可选重排）→ 组装 Prompt → 生成答案，并尽量带来源引用。

切分过碎会丢上下文；过长会引入噪声并浪费 token。常见做法是按段落或固定 token 窗口切分，并保留少量重叠。
